//
// Created by XL0002 on 2026/9/18.
//

#include "Animator.h"

void Animator::update(double dt) {
    //模拟状态机
    if (true) {
        // anims[0]->update(dt);
    }else {
        // anims[1]->update(dt);
    }
}
